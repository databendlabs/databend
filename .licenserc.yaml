header:
  license:
    spdx-id: Apache-2.0
    copyright-owner: Datafuse Labs
  paths-ignore:
    # ignore docs and helper tools
    - "scripts"
    - "docker"
    - "docs"
    - "website"
    - "tests"
    - "tools"
    - "tpch"
    # Ignore hidden files
    - ".cargo"
    - ".databend"
    - ".devcontainer"
    - ".github"
    - ".dockerignore"
    - ".gitignore"
    - ".gitattributes"
    # Ignore LICENSE and Makefile
    - "LICENSE"
    - "Makefile"
    # Ignore docs and generated files
    - "**/*.md"
    - "**/*.hbs"
    - "**/*.template"
    - "**/*.cue"
    - "**/*.json"
    - "**/*.sql"
    - "**/*.proto"
    - "**/*.yml"
    - "**/*.yaml"
    - "**/*.toml"
    - "**/*.lock"
    - "**/*.yapf"
    - "**/*.test"

  comment: on-failure
