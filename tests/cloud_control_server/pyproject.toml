[project]
name = "cloud-control-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "grpcio",
    "grpcio-reflection",
    "protobuf",
    "tomli; python_version < '3.11'",
]

[dependency-groups]
dev = [
    "grpcio-tools",
]
