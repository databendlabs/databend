statement ok
create or replace table t2(c varchar);

statement ok
insert into t2  select repeat('a', 1000000) from numbers(3000);

statement ok
select * from t2 ignore_result;

statement ok
copy into @unload_t2 from t2 file_format=(type=parquet);

statement ok
select $1 from @unload_t2(file_format=>'parquet') ignore_result;