query T
explain join select * from numbers(10000) where number in (select number from numbers(1000))
----
HashJoin: LEFT SEMI
├── Build
│   └── Scan: default.system.numbers (read rows: 1000)
└── Probe
    └── Scan: default.system.numbers (read rows: 10000)

query T
explain join select * from numbers(1000) where number in (select number from numbers(10000))
----
HashJoin: RIGHT SEMI
├── Build
│   └── Scan: default.system.numbers (read rows: 1000)
└── Probe
    └── Scan: default.system.numbers (read rows: 10000)
