statement ok
DROP TABLE IF EXISTS tbl

statement ok
CREATE TABLE tbl(i INTEGER);

statement ok
INSERT INTO tbl SELECT r FROM range(0 * 10000, 0 * 10000 + 2001) t(r);

statement ok
INSERT INTO tbl SELECT r FROM range(0 * 10000 + 2001, 0 * 10000 + 4001) t(r);

statement ok
INSERT INTO tbl SELECT r FROM range(0 * 10000 + 4001, 0 * 10000 + 5001) t(r);

statement ok
DELETE FROM tbl WHERE i >= 0 * 10000 AND i < 0 * 10000 + 2500;

statement ok
INSERT INTO tbl SELECT r FROM range(1 * 10000, 1 * 10000 + 2001) t(r);

statement ok
INSERT INTO tbl SELECT r FROM range(1 * 10000 + 2001, 1 * 10000 + 4001) t(r);

statement ok
INSERT INTO tbl SELECT r FROM range(1 * 10000 + 4001, 1 * 10000 + 5001) t(r);

statement ok
DELETE FROM tbl WHERE i >= 1 * 10000 AND i < 1 * 10000 + 2500;

statement ok
INSERT INTO tbl SELECT r FROM range(2 * 10000, 2 * 10000 + 2001) t(r);

statement ok
INSERT INTO tbl SELECT r FROM range(2 * 10000 + 2001, 2 * 10000 + 4001) t(r);

statement ok
INSERT INTO tbl SELECT r FROM range(2 * 10000 + 4001, 2 * 10000 + 5001) t(r);

statement ok
DELETE FROM tbl WHERE i >= 2 * 10000 AND i < 2 * 10000 + 2500;

statement ok
INSERT INTO tbl SELECT r FROM range(3 * 10000, 3 * 10000 + 2001) t(r);

statement ok
INSERT INTO tbl SELECT r FROM range(3 * 10000 + 2001, 3 * 10000 + 4001) t(r);

statement ok
INSERT INTO tbl SELECT r FROM range(3 * 10000 + 4001, 3 * 10000 + 5001) t(r);

statement ok
DELETE FROM tbl WHERE i >= 3 * 10000 AND i < 3 * 10000 + 2500;

statement ok
INSERT INTO tbl VALUES (60000);

statement ok
INSERT INTO tbl VALUES (60001);

query T
EXPLAIN ANALYZE SELECT i FROM tbl WHERE i = 60000;
----
TableScan
├── table: default.default.tbl
├── scan id: 0
├── output columns: [i (#0)]
├── read rows: 1
├── read size: < 1 KiB
├── partitions total: 10
├── partitions scanned: 1
├── pruning stats: [segments: <range pruning: 10 to 1 cost: 1 ms>, blocks: <range pruning: 1 to 1 cost: 1 ms, bloom pruning: 1 to 1 cost: 1 ms>]
├── push downs: [filters: [is_true(tbl.i (#0) = 60000)], limit: NONE]
└── estimated rows: 1.06

query I
SELECT i FROM tbl WHERE i = 60000;
----
60000

query T
EXPLAIN ANALYZE SELECT i FROM tbl WHERE i = 60001;
----
TableScan
├── table: default.default.tbl
├── scan id: 0
├── output columns: [i (#0)]
├── read rows: 1
├── read size: < 1 KiB
├── partitions total: 10
├── partitions scanned: 1
├── pruning stats: [segments: <range pruning: 10 to 1 cost: 1 ms>, blocks: <range pruning: 1 to 1 cost: 1 ms, bloom pruning: 1 to 1 cost: 1 ms>]
├── push downs: [filters: [is_true(tbl.i (#0) = 60001)], limit: NONE]
└── estimated rows: 1.06

query I
SELECT i FROM tbl WHERE i = 60001;
----
60001

query T
EXPLAIN ANALYZE SELECT i FROM tbl WHERE i = 0 * 10000 + 1000;
----
TableScan
├── table: default.default.tbl
├── scan id: 0
├── output columns: [i (#0)]
├── read rows: 0
├── read size: 0
├── partitions total: 10
├── partitions scanned: 0
├── pruning stats: [segments: <range pruning: 10 to 0 cost: 1 ms>]
├── push downs: [filters: [is_true(tbl.i (#0) = 1000)], limit: NONE]
└── estimated rows: 0.00

query I
SELECT i FROM tbl WHERE i = 0 * 10000 + 1000;
----

query T
EXPLAIN ANALYZE SELECT i FROM tbl WHERE i = 1 * 10000 + 1000;
----
TableScan
├── table: default.default.tbl
├── scan id: 0
├── output columns: [i (#0)]
├── read rows: 0
├── read size: 0
├── partitions total: 10
├── partitions scanned: 0
├── pruning stats: [segments: <range pruning: 10 to 0 cost: 1 ms>]
├── push downs: [filters: [is_true(tbl.i (#0) = 11000)], limit: NONE]
└── estimated rows: 1.06

query I
SELECT i FROM tbl WHERE i = 1 * 10000 + 1000;
----

query T
EXPLAIN ANALYZE SELECT i FROM tbl WHERE i = 2 * 10000 + 1000;
----
TableScan
├── table: default.default.tbl
├── scan id: 0
├── output columns: [i (#0)]
├── read rows: 0
├── read size: 0
├── partitions total: 10
├── partitions scanned: 0
├── pruning stats: [segments: <range pruning: 10 to 0 cost: 1 ms>]
├── push downs: [filters: [is_true(tbl.i (#0) = 21000)], limit: NONE]
└── estimated rows: 1.06

query I
SELECT i FROM tbl WHERE i = 2 * 10000 + 1000;
----

query T
EXPLAIN ANALYZE SELECT i FROM tbl WHERE i = 3 * 10000 + 1000;
----
TableScan
├── table: default.default.tbl
├── scan id: 0
├── output columns: [i (#0)]
├── read rows: 0
├── read size: 0
├── partitions total: 10
├── partitions scanned: 0
├── pruning stats: [segments: <range pruning: 10 to 0 cost: 1 ms>]
├── push downs: [filters: [is_true(tbl.i (#0) = 31000)], limit: NONE]
└── estimated rows: 1.06

query I
SELECT i FROM tbl WHERE i = 3 * 10000 + 1000;
----

query I
SELECT i FROM tbl WHERE i = 3000;
----
3000

query I
SELECT i FROM tbl WHERE i = 13000;
----
13000

query I
SELECT i FROM tbl WHERE i = 23000;
----
23000

query I
SELECT i FROM tbl WHERE i = 4500;
----
4500

query I
SELECT i FROM tbl WHERE i = 14500;
----
14500

query I
SELECT i FROM tbl WHERE i = 24500;
----
24500

