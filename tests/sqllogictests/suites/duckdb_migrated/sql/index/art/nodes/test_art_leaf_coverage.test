statement ok
DROP TABLE IF EXISTS leaf_merge_1

statement ok
DROP TABLE IF EXISTS leaf_merge_2

statement ok
DROP TABLE IF EXISTS tbl_dup_ser

# Test the deserialization of leaves.
# test different paths in the Leaf::Merge functionality

statement ok
CREATE TABLE leaf_merge_1 (id UINT32, id2 INT64);

statement ok
INSERT INTO leaf_merge_1 SELECT 2047, 2047 FROM range(0, 10);

statement ok
CREATE TABLE leaf_merge_2 (id UINT32, id2 INT64);

statement ok
INSERT INTO leaf_merge_2 VALUES (2047, 2047)

# test merging leaves with same key

statement ok
CREATE TABLE tbl_dup_ser (id INTEGER);

statement ok
INSERT INTO tbl_dup_ser SELECT 12345 FROM range(0, 11);

