statement ok
DROP TABLE IF EXISTS depdelay_minutes

statement ok
DROP TABLE IF EXISTS lineitem

statement ok
DROP TABLE IF EXISTS s1.my_lineitem

statement ok
DROP TABLE IF EXISTS s1.orders

statement ok
CREATE TABLE depdelay_minutes(depdelay_minutes INTEGER);

statement error
SELECT * FROM depdelay

statement error
SELECT depdelay FROM depdelay_minutes

statement ok
CREATE TABLE lineitem(i INTEGER);

statement error
SELECT * FROM li

statement error
SELECT * FROM lineitem_long

statement error
select jaro_winkler_('x', 'y');

statement ok
create table s1.my_lineitem(i int);

# exact match: show it

statement error
select * from my_lineitem;

statement error
select * from m_lineitem;

statement ok
create table s1.orders(i int)

statement error
select * from ord
