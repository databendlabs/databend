statement ok
DROP TABLE IF EXISTS integers

# TODO: unsupported feature
# statement ok
# DROP TABLE IF EXISTS integers2

statement ok
CREATE TABLE integers AS SELECT i FROM range(0, 200000) AS r(i);

# TODO: unsupported feature
# statement ok
# CREATE TABLE integers2 AS SELECT i, i % 4 AS j FROM range(0, 200000) AS r(i);
