statement ok
DROP TABLE IF EXISTS bools

statement ok
CREATE TABLE bools(b BOOL)

statement ok
INSERT INTO bools SELECT CASE WHEN i%2=0 THEN NULL ELSE i%7=0 OR i%3=0 END b FROM range(0, 10000) tbl(i);

