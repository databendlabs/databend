statement ok
DROP TABLE IF EXISTS t

# TODO: unsupported feature
# query I
# select * FROM '{DATA_DIR}/csv/null_terminator.csv'
# ----

# TODO: unsupported feature
# query I
# select * FROM read_csv('{DATA_DIR}/csv/null_terminator.csv', header = 0)
# ----
# a\0b

# TODO: unsupported feature
# query I
# select * FROM read_csv('{DATA_DIR}/csv/null_terminator.csv', header = 0, escape = '')
# ----
# a\0b

# TODO: unsupported feature
# query I
# select * FROM read_csv('{DATA_DIR}/csv/null_terminator.csv', header = 0, delim = '\t', quote = '', escape = '')
# ----
# a\0b

statement ok
create or replace table t as (FROM (VALUES ('a' || chr(0) || 'b')) t(i));

# TODO: unsupported feature
# statement ok
# copy t to '{TEMP_DIR}/csv2tsv.tsv' (header false, delimiter '\t', escape '', quote '');

# TODO: unsupported feature
# query I
# select * FROM read_csv('{TEMP_DIR}/csv2tsv.tsv', header = 0)
# ----
# a\0b
