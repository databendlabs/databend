# TODO: unsupported feature
# query III
# select * FROM read_csv('{DATA_DIR}/csv/error/mid_null.csv', delim = ';',
#      columns = {'a':'integer','b':'integer','c':'integer'}, auto_detect = false, header = true, ignore_errors = true, strict_mode=True)
# ----
# 1	2	3
# 1	2	3
