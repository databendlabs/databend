statement ok
DROP TABLE IF EXISTS tbl

statement ok
DROP TABLE IF EXISTS tbl2

# partitions
statement ok
CREATE TABLE tbl AS SELECT i DIV 1000 AS partition, i FROM range(0, 10000) t(i)

statement ok
CREATE TABLE tbl2 AS SELECT (DATE '2000-01-01' + INTERVAL (i DIV 2000) YEAR)::DATE AS part1, i % 2 AS part2, i FROM range(0, 10000) t(i)
