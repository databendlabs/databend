statement ok
DROP TABLE IF EXISTS encrypted.tbl

statement ok
DROP TABLE IF EXISTS unencrypted.tbl

# workaround - alternative verify always forces the latest storage
# We need httpfs to do encrypted writes
