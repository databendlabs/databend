statement ok
DROP TABLE IF EXISTS db1.myschema

statement ok
DROP TABLE IF EXISTS db1.test

statement ok
DROP TABLE IF EXISTS hello

# did you mean errors with an attached database

statement ok
CREATE TABLE hello(i INTEGER);

statement ok
CREATE TABLE db1.test(a INTEGER);

statement error
SELECT * FROM blablabla;

statement error
SELECT * FROM blablabla;

# what if we switch the default database?

statement ok
USE db1

statement ok
SELECT * FROM test

statement error
SELECT * FROM blablabla

statement error
SELECT * FROM hello;

# what if we switch default database AND default schema?

statement ok
SELECT * FROM test;

statement error
SELECT * FROM hello;
