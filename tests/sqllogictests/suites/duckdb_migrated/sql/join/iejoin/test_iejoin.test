statement ok
DROP TABLE IF EXISTS issue3486

statement ok
DROP TABLE IF EXISTS test_big

statement ok
DROP TABLE IF EXISTS test_small

# Subquery/CTE

# RTEs are not (yet) supported
# so this should work, but not trigger IEJoin in the physical plan.

# Fix missing continue statement in right join handler

# internal issue 5197

