statement ok
DROP TABLE IF EXISTS large_structs

statement ok
DROP TABLE IF EXISTS nested_structs

statement ok
DROP TABLE IF EXISTS string_structs

statement ok
DROP TABLE IF EXISTS test_structs

statement ok
CREATE TABLE test_structs(i TUPLE(a integer, b bool));

# Ensure it works with case insensitive struct field names

# This can also produce a filter pushdown

# String comparisons

statement ok
CREATE TABLE string_structs(s TUPLE(a varchar, b varchar));

# Large collection of structs

statement ok
CREATE TABLE large_structs(i TUPLE(a integer, b bool));

# Nested structs

statement ok
CREATE TABLE nested_structs(s TUPLE(a TUPLE(b integer, c bool), d TUPLE(e integer, f varchar)));

# Parquet test
