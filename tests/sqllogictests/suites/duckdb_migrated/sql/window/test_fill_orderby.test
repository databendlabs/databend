#
# Error checks
#
# Only one ordering allowed
# TODO: unsupported feature
# statement error
# select fill(i order by 10-i, i * i) over (order by i) from range(0, 3) tbl(i);

# TODO: unsupported feature
# statement error
# select fill(i::VARCHAR order by i) over (order by i) from range(0, 3) tbl(i);

# TODO: unsupported feature
# statement error
# select fill(i order by i::VARCHAR) over (order by i) from range(0, 3) tbl(i);

# TODO: unsupported feature
# query III
# with source as (
# 	select 
# 		i, 
# 		i * 3 % 5 as permuted,
# 		if(permuted = 0, NULL, permuted) as missing
# 	from range(0, 5) tbl(i)
# )
# select
# 	i,
# 	permuted,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled <> permuted
# ----

# TODO: unsupported feature
# query III
# with source as (
# 	select 
# 		i, 
# 		i * 3 % 5 as permuted,
# 		if(permuted = 1, NULL, permuted) as missing
# 	from range(0, 5) tbl(i)
# )
# select
# 	i,
# 	permuted,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled <> permuted
# ----

# TODO: unsupported feature
# query III
# with source as (
# 	select 
# 		i, 
# 		i * 3 % 5 as permuted,
# 		if(permuted = 2, NULL, permuted) as missing
# 	from range(0, 5) tbl(i)
# )
# select
# 	i,
# 	permuted,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled <> permuted
# ----

# TODO: unsupported feature
# query III
# with source as (
# 	select 
# 		i, 
# 		i * 3 % 5 as permuted,
# 		if(permuted = 3, NULL, permuted) as missing
# 	from range(0, 5) tbl(i)
# )
# select
# 	i,
# 	permuted,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled <> permuted
# ----

# TODO: unsupported feature
# query III
# with source as (
# 	select 
# 		i, 
# 		i * 3 % 5 as permuted,
# 		if(permuted = 4, NULL, permuted) as missing
# 	from range(0, 5) tbl(i)
# )
# select
# 	i,
# 	permuted,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled <> permuted
# ----


# Single values

# TODO: unsupported feature
# query III
# with source as (
# 	select 
# 		i, 
# 		i * 3 % 5 as permuted,
# 		if(permuted > 0, NULL, permuted) as missing
# 	from range(0, 5) tbl(i)
# )
# select
# 	i,
# 	permuted,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled <> permuted
# ----
# 1	3	0
# 2	1	0
# 3	4	0
# 4	2	0

# No values in partition

# TODO: unsupported feature
# query III
# with source as (
# 	select 
# 		i, 
# 		i * 5 % 11 as permuted,
# 		if(permuted < 6, NULL, permuted) as missing
# 	from range(0, 11) tbl(i)
# )
# select
# 	i,
# 	permuted,
# 	fill(missing order by permuted) over (partition by permuted // 5 order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----
# 0	0	NULL
# 3	4	NULL
# 5	3	NULL
# 7	2	NULL
# 9	1	NULL

# No values in partition chunk

# TODO: unsupported feature
# statement ok
# with null_chunk as (
# 	select 
# 		1 as p, 
# 		s,
# 		NULL::DOUBLE as v,
# 	from range(0, 2050) tbl(s)
# 	union all
# 	select
# 		2 as p, 
# 		s,
# 		s::DOUBLE as v,
# 	from range(0, 16) tbl(s)
# )
# select 
# 	p, 
# 	s, 
# 	fill(v order by -s) over(partition by p order by s) as f,
# from null_chunk

# Order by duplication optimisation

# TODO: unsupported feature
# query III
# with null_chunk as (
# 	select 
# 		1 as p, 
# 		s,
# 		NULL::DOUBLE as v,
# 	from range(0, 8) tbl(s)
# 	union all
# 	select
# 		2 as p, 
# 		s,
# 		s::DOUBLE as v,
# 	from range(0, 8) tbl(s)
# )
# select 
# 	p, 
# 	s, 
# 	fill(v order by s) over(partition by p order by s) as f,
# from null_chunk
# order by all
# ----
# 1	0	NULL
# 1	1	NULL
# 1	2	NULL
# 1	3	NULL
# 1	4	NULL
# 1	5	NULL
# 1	6	NULL
# 1	7	NULL
# 2	0	0.0
# 2	1	1.0
# 2	2	2.0
# 2	3	3.0
# 2	4	4.0
# 2	5	5.0
# 2	6	6.0
# 2	7	7.0

# Outside valid sort values

# TODO: unsupported feature
# query III
# with source as (
# 	select 
# 		i, 
# 		i * 5 % 11 as permuted,
# 		if(permuted = 2, NULL, permuted) as missing,
# 		if(permuted < 4, NULL, permuted) as unsorted,
# 	from range(0, 11) tbl(i)
# )
# select
# 	i,
# 	permuted,
# 	fill(missing order by unsorted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----
# 7	2	NULL

# NULL sort key coverage

# TODO: unsupported feature
# query III
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 0, NULL, permuted) as missing
# 	from (
# 		from range(0, 5) tbl(i)
# 		union all 
# 		select NULL::INTEGER as i
# 	) t(i)
# )
# select
# 	i,
# 	permuted,
# 	fill(missing order by permuted asc nulls first) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# ----

# TODO: unsupported feature
# query III
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 4, NULL, permuted) as missing
# 	from (
# 		from range(0, 5) tbl(i)
# 		union all 
# 		select NULL::INTEGER as i
# 	) t(i)
# )
# select
# 	i,
# 	permuted,
# 	fill(missing order by permuted asc nulls last) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# ----

# TODO: unsupported feature
# query III
# select
# 	i,
# 	permuted,
# 	fill(missing order by permuted asc nulls last) over (order by i) as filled
# from (values
# 	(0, 1, NULL),
# 	(1, NULL, 0)
# ) source(i, missing, permuted)
# order by i
# ----
# 0	NULL	1
# 1	0	NULL

# TODO: unsupported feature
# query III
# select
# 	i,
# 	permuted,
# 	fill(missing order by permuted asc nulls first) over (order by i) as filled
# from (values
# 	(0, NULL, 2),
# 	(1, 0, NULL),
# 	(2, 1, 1),
# ) source(i, missing, permuted)
# order by i
# ----
# 0	2	1
# 1	NULL	0
# 2	1	1

#
# Unusable values
#
# If we use an unsuable value, the interpolation will produce strange values

# Infinity/NaN

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 0, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select 'infinity'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 1, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select 'infinity'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 2, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select 'infinity'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 3, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select 'infinity'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 4, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select 'infinity'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 0, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select '-infinity'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 1, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select '-infinity'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 2, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select '-infinity'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 3, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select '-infinity'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 4, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select '-infinity'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 0, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select 'NaN'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 1, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select 'NaN'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 2, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select 'NaN'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 3, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select 'NaN'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 4, NULL, permuted) as missing
# 	from (
# 		select range::float as i from range(0, 5)
# 		union all 
# 		select 'NaN'::float as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 0, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select 'infinity'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 1, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select 'infinity'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 2, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select 'infinity'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 3, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select 'infinity'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 4, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select 'infinity'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 0, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select '-infinity'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 1, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select '-infinity'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 2, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select '-infinity'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 3, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select '-infinity'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 4, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select '-infinity'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 0, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select 'NaN'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 1, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select 'NaN'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 2, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select 'NaN'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 3, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select 'NaN'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	select 
# 		i, 
# 		(i + 1) * 3 % 5 as permuted,
# 		if(permuted = 4, NULL, permuted) as missing
# 	from (
# 		select range::double as i from range(0, 5)
# 		union all 
# 		select 'NaN'::double as i
# 	) t(i)
# )
# select 
# 	i,
# 	fill(missing order by permuted) over (order by i) as filled
# from source
# qualify filled is distinct from permuted
# order by i
# ----




# Temporal infinities

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::date as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::date as permuted,
# 			if(i = 0, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'infinity'::date as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::date as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::date as permuted,
# 			if(i = 1, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'infinity'::date as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::date as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::date as permuted,
# 			if(i = 2, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'infinity'::date as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::date as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::date as permuted,
# 			if(i = 3, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'infinity'::date as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::date as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::date as permuted,
# 			if(i = 4, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'infinity'::date as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::date as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::date as permuted,
# 			if(i = 0, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'-infinity'::date as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::date as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::date as permuted,
# 			if(i = 1, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'-infinity'::date as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::date as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::date as permuted,
# 			if(i = 2, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'-infinity'::date as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::date as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::date as permuted,
# 			if(i = 3, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'-infinity'::date as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::date as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::date as permuted,
# 			if(i = 4, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'-infinity'::date as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::timestamp as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::timestamp as permuted,
# 			if(i = 0, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'infinity'::timestamp as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::timestamp as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::timestamp as permuted,
# 			if(i = 1, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'infinity'::timestamp as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::timestamp as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::timestamp as permuted,
# 			if(i = 2, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'infinity'::timestamp as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::timestamp as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::timestamp as permuted,
# 			if(i = 3, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'infinity'::timestamp as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::timestamp as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::timestamp as permuted,
# 			if(i = 4, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'infinity'::timestamp as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::timestamp as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::timestamp as permuted,
# 			if(i = 0, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'-infinity'::timestamp as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::timestamp as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::timestamp as permuted,
# 			if(i = 1, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'-infinity'::timestamp as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::timestamp as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::timestamp as permuted,
# 			if(i = 2, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'-infinity'::timestamp as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::timestamp as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::timestamp as permuted,
# 			if(i = 3, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'-infinity'::timestamp as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----

# TODO: unsupported feature
# query II
# with source as (
# 	(
# 		select 
# 			('2025-01-01'::DATE + INTERVAL (i) DAY)::timestamp as t, 
# 			('2025-01-01'::DATE + INTERVAL ((i + 1) * 3 % 5) DAY)::timestamp as permuted,
# 			if(i = 4, NULL, permuted) as missing
# 		from range(0, 5) tbl(i)
# 	)
# 	union all
# 	(
# 		select
# 			'-infinity'::timestamp as t,
# 			t as permuted,
# 			permuted as missing
# 	)
# )
# select 
# 	t,
# 	fill(missing order by permuted) over (order by t) as filled
# from source
# qualify filled is distinct from permuted
# order by t
# ----
