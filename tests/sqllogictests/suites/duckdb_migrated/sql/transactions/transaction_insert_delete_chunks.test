statement ok
DROP TABLE IF EXISTS integers

statement ok
CREATE TABLE integers(i INTEGER);

statement ok
INSERT INTO integers FROM range(0, 1000);

statement ok
BEGIN TRANSACTION

statement ok
BEGIN TRANSACTION

# con1 inserts rows 1000..3000

statement ok
INSERT INTO integers FROM range(1000, 3000);

statement ok
COMMIT

# con2 deletes the rows 1000.3000

query I
DELETE FROM integers
----
3000

statement ok
COMMIT

query III
SELECT MIN(i), MAX(i), COUNT(*) FROM integers
----
NULL NULL 0

# now do the same but with bigger chunks

statement ok
INSERT INTO integers FROM range(0, 10000);

statement ok
BEGIN TRANSACTION

statement ok
BEGIN TRANSACTION

# con1 inserts rows 1000..3000

statement ok
INSERT INTO integers FROM range(10000, 30000);

statement ok
COMMIT

# con2 deletes the rows 1000.3000

query I
DELETE FROM integers
----
30000

statement ok
COMMIT

query III
SELECT MIN(i), MAX(i), COUNT(*) FROM integers
----
NULL NULL 0
