# TODO: unsupported feature
# statement ok
# DROP TABLE IF EXISTS tbl

# TODO: unsupported feature
# statement ok
# CREATE TABLE tbl AS SELECT * FROM range(0, 10000) tbl(i) UNION ALL SELECT NULL

# TODO: unsupported feature
# query I
# SELECT SUM(i) FROM tbl
# UNION ALL
# SELECT COUNT(i) FROM tbl
# UNION ALL
# SELECT AVG(i) FROM tbl
# UNION ALL
# SELECT AVG(i) + SUM(i) FROM tbl
# ORDER BY 1
# ----
# 4999.5
# 10000.0
# 49995000.0
# 49999999.5
