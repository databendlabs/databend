# TODO: unsupported feature
# statement ok
# DROP TABLE IF EXISTS integers

# TODO: unsupported feature
# statement ok
# CREATE TABLE integers(
# 	i INT UNIQUE,
# 	j INT DEFAULT 0,
# 	k INT DEFAULT 0
# );

# TODO: unsupported feature
# statement ok
# INSERT INTO integers(i) SELECT i FROM range(0, 5000) tbl(i);

# TODO: unsupported feature
# statement ok
# INSERT INTO integers(i, j)
# SELECT i % 5, i
# FROM range(0, 5000) tbl(i) ON CONFLICT DO UPDATE SET
# 	j = excluded.j,
# 	k = excluded.i;
