# TODO: unsupported feature
# statement ok
# DROP TABLE IF EXISTS test

# load the DB from disk
# TODO: unsupported feature
# statement ok
# CREATE TABLE test(id INTEGER PRIMARY KEY, col INTEGER);

# TODO: unsupported feature
# statement ok
# INSERT INTO test SELECT i::VARCHAR id, 1 b FROM range(0, 5000) tbl(i)

# TODO: unsupported feature
# statement ok
# INSERT INTO test SELECT (5000 + i)::VARCHAR id, 2 b FROM range(0, 5000) tbl(i)

# TODO: unsupported feature
# statement ok
# CHECKPOINT

# TODO: unsupported feature
# query I
# SELECT compression FROM pragma_storage_info('test') WHERE segment_type ILIKE 'INTEGER' LIMIT 1
# ----
# RLE

# TODO: unsupported feature
# query IIIIII
# SELECT MIN(id), MAX(id), SUM(col), MIN(col), MAX(col), COUNT(*) FROM test WHERE id='5000'
# ----
# 5000	5000	2	2	2	1
