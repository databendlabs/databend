# TODO: unsupported feature
# statement ok
# DROP TABLE IF EXISTS null_byte

# load the DB from disk
# TODO: unsupported feature
# statement ok
# CREATE TABLE null_byte AS SELECT concat('goo', chr(0), i) AS v FROM range(0, 10000) tbl(i)

# TODO: unsupported feature
# query II
# SELECT MIN(v), MAX(v) FROM null_byte
# ----
# goo\00	goo\09999

# TODO: unsupported feature
# query I
# SELECT * FROM null_byte WHERE v=concat('goo', chr(0), 42)
# ----
# goo\042

# null byte in index

# TODO: unsupported feature
# statement ok
# CREATE INDEX i_index ON null_byte(v)

# TODO: unsupported feature
# query I
# SELECT * FROM null_byte WHERE v=concat('goo', chr(0), 42)
# ----
# goo\042

# TODO: unsupported feature
# statement ok
# DROP TABLE null_byte

# TODO: unsupported feature
# statement ok
# CREATE TABLE null_byte AS SELECT concat('goo', chr(0), i) AS v FROM range(0, 10000) tbl(i)

# TODO: unsupported feature
# query II
# SELECT MIN(v), MAX(v) FROM null_byte
# ----
# goo\00	goo\09999

# TODO: unsupported feature
# query I
# SELECT * FROM null_byte WHERE v=concat('goo', chr(0), 42)
# ----
# goo\042

# null byte in index

# TODO: unsupported feature
# statement ok
# CREATE INDEX i_index ON null_byte(v)

# TODO: unsupported feature
# query I
# SELECT * FROM null_byte WHERE v=concat('goo', chr(0), 42)
# ----
# goo\042

# TODO: unsupported feature
# statement ok
# DROP TABLE null_byte

# TODO: unsupported feature
# statement ok
# CREATE TABLE null_byte AS SELECT concat('goo', chr(0), i) AS v FROM range(0, 10000) tbl(i)

# TODO: unsupported feature
# query II
# SELECT MIN(v), MAX(v) FROM null_byte
# ----
# goo\00	goo\09999

# TODO: unsupported feature
# query I
# SELECT * FROM null_byte WHERE v=concat('goo', chr(0), 42)
# ----
# goo\042

# null byte in index

# TODO: unsupported feature
# statement ok
# CREATE INDEX i_index ON null_byte(v)

# TODO: unsupported feature
# query I
# SELECT * FROM null_byte WHERE v=concat('goo', chr(0), 42)
# ----
# goo\042

# TODO: unsupported feature
# statement ok
# DROP TABLE null_byte

# TODO: unsupported feature
# statement ok
# CREATE TABLE null_byte AS SELECT concat('goo', chr(0), i) AS v FROM range(0, 10000) tbl(i)

# TODO: unsupported feature
# query II
# SELECT MIN(v), MAX(v) FROM null_byte
# ----
# goo\00	goo\09999

# TODO: unsupported feature
# query I
# SELECT * FROM null_byte WHERE v=concat('goo', chr(0), 42)
# ----
# goo\042

# null byte in index

# TODO: unsupported feature
# statement ok
# CREATE INDEX i_index ON null_byte(v)

# TODO: unsupported feature
# query I
# SELECT * FROM null_byte WHERE v=concat('goo', chr(0), 42)
# ----
# goo\042

# TODO: unsupported feature
# statement ok
# DROP TABLE null_byte
