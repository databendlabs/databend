# TODO: unsupported feature
# statement ok
# DROP TABLE IF EXISTS vals

# load the DB from disk
# TODO: unsupported feature
# statement ok
# CREATE TABLE vals(i INTEGER)

# TODO: unsupported feature
# statement ok
# INSERT INTO vals SELECT CASE WHEN i % 2 = 0 THEN NULL ELSE i END FROM range(0, 200000) tbl(i)

# TODO: unsupported feature
# query IIII
# SELECT MIN(i), MAX(i), COUNT(i), COUNT(*) FROM vals
# ----
# 1	199999	100000	200000

# TODO: unsupported feature
# statement ok
# CHECKPOINT

# TODO: unsupported feature
# query IIII
# SELECT MIN(i), MAX(i), COUNT(i), COUNT(*) FROM vals
# ----
# 1	199999	100000	200000

# TODO: unsupported feature
# statement ok
# INSERT INTO vals SELECT * FROM vals;

# TODO: unsupported feature
# query IIII
# SELECT MIN(i), MAX(i), COUNT(i), COUNT(*) FROM vals
# ----
# 1	199999	200000	400000
