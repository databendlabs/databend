# TODO: unsupported feature
# statement ok
# DROP TABLE IF EXISTS tbl

# load the DB from disk
# TODO: unsupported feature
# statement ok
# CREATE TABLE tbl (a STRUCT("id" VARCHAR), b STRUCT("id" VARCHAR));

# TODO: unsupported feature
# statement ok
# INSERT INTO tbl SELECT {'id': LPAD(i::VARCHAR, 4, '0')}, {'id': 'abc'} FROM range(0, 10000) t(i)

# TODO: unsupported feature
# query I
# SELECT COUNT(*) FROM (SELECT * FROM tbl WHERE b.id='abc') t
# ----
# 10000

# TODO: unsupported feature
# query I
# SELECT COUNT(*) FROM (SELECT * FROM tbl WHERE b.id='abc') t
# ----
# 10000

# TODO: unsupported feature
# statement ok
# INSERT INTO tbl SELECT {'id': LPAD((i + 10000)::VARCHAR, 4, '0')}, {'id': 'bcd'} FROM range(0, 10000) t(i)

# TODO: unsupported feature
# query I
# SELECT COUNT(*) FROM (SELECT * FROM tbl WHERE b.id='bcd') t
# ----
# 10000
