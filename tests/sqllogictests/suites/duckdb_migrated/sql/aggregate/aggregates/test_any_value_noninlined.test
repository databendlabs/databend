statement ok
DROP TABLE IF EXISTS test_any_value_noninlined_tbl

statement ok
CREATE TABLE test_any_value_noninlined_tbl(a INTEGER, b VARCHAR)

statement ok
INSERT INTO test_any_value_noninlined_tbl VALUES (1, NULL), (2, 'thisisalongstring'), (3, 'thisisalsoalongstring')

# non-grouped aggregate

query T
SELECT ANY_VALUE(b) FROM test_any_value_noninlined_tbl
----
thisisalongstring

query T
SELECT ANY_VALUE(b) FROM test_any_value_noninlined_tbl WHERE a=2
----
thisisalongstring

query T
SELECT ANY_VALUE(b) FROM test_any_value_noninlined_tbl WHERE a=1
----
NULL

query T
SELECT ANY_VALUE(b) FROM test_any_value_noninlined_tbl WHERE a=1 GROUP BY a
----
NULL

query T
SELECT ANY_VALUE(b) FROM test_any_value_noninlined_tbl WHERE a=0
----
NULL

query T
SELECT ANY_VALUE(b) FROM test_any_value_noninlined_tbl WHERE a=0 GROUP BY b
----

# grouped aggregate

query IT
SELECT a, ANY_VALUE(b) FROM test_any_value_noninlined_tbl GROUP BY a ORDER BY a
----
1 NULL
2 thisisalongstring
3 thisisalsoalongstring

query I
SELECT ANY_VALUE(i) FROM (VALUES (NULL::INT32)) tbl(i)
----
NULL
