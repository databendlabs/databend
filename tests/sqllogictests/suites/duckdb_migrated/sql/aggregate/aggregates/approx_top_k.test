statement ok
DROP TABLE IF EXISTS integers

statement ok
CREATE TABLE integers AS SELECT i%5 as even_groups, log(1 + i*i)::int as skewed_groups  FROM range(0, 10000) t(i);

# select all groups

# select a subset of the groups
# they should be ordered from most frequent to least frequent

# varchars

# lists

# structs

statement error
select approx_top_k(i, 0) from range(0, 5) t(i)

statement error
select approx_top_k(i, -1) from range(0, 5) t(i)

statement error
select approx_top_k(i, 999999999999999) from range(0, 5) t(i)

statement error
select approx_top_k(i, NULL) from range(0, 5) t(i)
