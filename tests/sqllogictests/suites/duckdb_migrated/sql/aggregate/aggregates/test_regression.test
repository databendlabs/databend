statement ok
DROP TABLE IF EXISTS aggr

#Corner cases

statement error
select regr_avgx()

statement error
select regr_avgx(*)

statement error
select regr_avgy()

statement error
select regr_avgy(*)

statement error
select regr_count()

statement error
select regr_count(*)

statement error
select regr_slope()

statement error
select regr_slope(*)

statement error
select regr_r2()

#Corner cases

statement error
select regr_r2(0, 1e230*i) from range(0, 5) tbl(i);

statement error
select regr_r2(1e230*i, i) from range(0, 5) tbl(i);

statement error
select regr_r2(*)

statement error
select regr_sxx()

statement error
select regr_sxx(0, 2e230*i) from range(0, 5) tbl(i)

statement error
select regr_syy()

statement error
select regr_sxy(*)

statement error
select regr_sxy()

# Window Function

