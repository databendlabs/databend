statement ok
DROP TABLE IF EXISTS test_covar_integers

# test incorrect usage of COVAR_POP function
statement error
SELECT COVAR_POP()

statement error
SELECT COVAR_POP(1, 2, 3)

statement error
SELECT COVAR_POP(COVAR_POP(1))

statement error
SELECT COVAR_SAMP()

statement error
SELECT COVAR_SAMP(1, 2, 3)

statement error
SELECT COVAR_SAMP(COVAR_SAMP(1))

# test sample covariance on scalar values

# test population covariance on a sequence

# test population covariance on a set of values

statement ok
CREATE TABLE test_covar_integers(x INTEGER, y INTEGER);

statement ok
INSERT INTO test_covar_integers VALUES (10,NULL), (10,11), (20,22), (25,NULL), (30,35)

# test covar on empty set

query RR
SELECT COVAR_POP(x,y), COVAR_SAMP(x,y) FROM test_covar_integers WHERE x > 100
----
NULL NULL

# test covar with only null inputs

