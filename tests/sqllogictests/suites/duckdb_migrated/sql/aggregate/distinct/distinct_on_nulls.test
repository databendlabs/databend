statement ok
DROP TABLE IF EXISTS distinct_on_test

statement ok
DROP TABLE IF EXISTS integers

statement ok
CREATE TABLE integers(i INTEGER, j INTEGER);

statement ok
INSERT INTO integers VALUES (2, 3), (4, 5), (2, NULL), (NULL, NULL);

# multi-way sort and ties

