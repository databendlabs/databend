# TODO: unsupported feature
# query II rowsort
# select t.*
# from (
#   (values (1), (2)) as t1 (a)
#   cross join
#   (values (3), (4)) as t2 (b)
# ) as t
# ----
# 1	3
# 1	4
# 2	3
# 2	4

# column alias

# TODO: unsupported feature
# query II rowsort
# select x, y
# from (
#   (values (1), (2)) as t1 (a)
#   cross join
#   (values (3), (4)) as t2 (b)
# ) as t(x, y)
# ----
# 1	3
# 1	4
# 2	3
# 2	4

# TODO: unsupported feature
# statement error
# from (
#   (values (1), (2)) as t1 (a)
#   cross join
#   (values (3), (4)) as t2 (b)
# ) as t(x, y, z)
