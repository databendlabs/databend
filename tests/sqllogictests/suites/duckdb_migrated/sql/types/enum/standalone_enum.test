statement ok
DROP TABLE IF EXISTS test

statement error
SELECT 'hello'::ENUM;

statement error
SELECT 'hello'::ENUM(42);

statement error
SELECT 'hello'::ENUM('zzz', 42);

statement error
SELECT 'hello'::ENUM(foobar 42);
