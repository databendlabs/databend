# TODO: unsupported feature
# statement ok
# SELECT a from (SELECT STRUCT_PACK(a := 42, b := 43) as a) as t

# TODO: unsupported feature
# statement ok
# SELECT a from (SELECT STRUCT_PACK(a := NULL, b := 43) as a) as t

# TODO: unsupported feature
# statement ok
# SELECT a from (SELECT STRUCT_PACK(a := NULL) as a) as t

# TODO: unsupported feature
# statement ok
# SELECT a from (SELECT STRUCT_PACK(a := i, b := i) as a FROM range(0, 10000) tbl(i)) as t

# TODO: unsupported feature
# statement ok
# SELECT a from (SELECT STRUCT_PACK(a := LIST_VALUE(1,2,3), b := i) as a FROM range(0, 10000) tbl(i)) as t
