statement ok
DROP TABLE IF EXISTS t

statement ok
DROP TABLE IF EXISTS t0

# Happy path
statement ok
CREATE TABLE t(i INTEGER, j INTEGER)

statement ok
INSERT INTO t VALUES (1, 1), (2, 2)

# TODO: unsupported feature
# statement ok
# ALTER TABLE t ALTER COLUMN j SET NOT NULL

# TODO: result differs
# statement error
# INSERT INTO t VALUES (3, NULL)

# TODO: unsupported feature
# statement ok
# ALTER TABLE t ALTER COLUMN j SET NOT NULL

# TODO: result differs
# statement error
# INSERT INTO t VALUES (3, NULL)

# TODO: result differs
# query II
# SELECT * FROM t
# ----
# 3 NULL
# 3 NULL
# 1 1
# 2 2

# Large table

# TODO: unsupported feature
# statement ok
# INSERT INTO t SELECT 5,5 from range(0, 65534)

# TODO: result differs
# statement ok
# ALTER TABLE t ALTER COLUMN j SET NOT NULL

# TODO: result differs
# statement error
# INSERT INTO t VALUES (6, NULL)

query I
SELECT COUNT(*) FROM t WHERE j IS NULL
----
0

# SET NOT NULL failed if contains null value

statement ok
DROP TABLE IF EXISTS t

statement ok
CREATE TABLE t(i INTEGER, j INTEGER)

statement ok
INSERT INTO t VALUES (1, 1), (2, 2), (3, null)

statement ok
INSERT INTO t SELECT 4,4 FROM range(0, 65536)

statement error
ALTER TABLE t ALTER COLUMN j SET NOT NULL

statement ok
INSERT INTO t VALUES (5, null)

query II
SELECT * FROM t WHERE j IS NULL
----
3 NULL
5 NULL

# Insert null before&after drop constraint

statement ok
DROP TABLE IF EXISTS t

statement ok
CREATE TABLE t(i INTEGER, j INTEGER)

# TODO: unsupported feature
# statement ok
# INSERT INTO t SELECT 1,1 FROM range(0, 65536)

# Set not null

# TODO: result differs
# statement ok
# ALTER TABLE t ALTER COLUMN j SET NOT NULL

# Failed to insert before drop constraint

# TODO: unsupported feature
# statement error
# INSERT INTO t VALUES (2, null)

# TODO: unsupported feature
# statement ok
# ALTER TABLE t ALTER COLUMN j DROP NOT NULL

# Insert after drop constraint

statement ok
INSERT INTO t VALUES (3, null)

# Only 1 null

# TODO: unsupported feature
# query II
# SELECT * FROM t WHERE j IS NULL
# ----
# 2 NULL
# 3 NULL

# Basic test with generated column

# TODO: unsupported feature
# statement ok
# CREATE TABLE t0(c0 AS (1), c1 INT);

# TODO: unsupported feature
# statement ok
# ALTER TABLE t0 ALTER c1 SET NOT NULL;

# Insert null before&after drop constraint with generated column

# TODO: unsupported feature
# statement ok
# DROP TABLE IF EXISTS t

# TODO: unsupported feature
# statement ok
# CREATE TABLE t(i AS (1), j INTEGER)

# TODO: unsupported feature
# statement ok
# INSERT INTO t SELECT 1 FROM range(0, 65536)

# Set not null

# TODO: unsupported feature
# statement error
# ALTER TABLE t ALTER COLUMN i SET NOT NULL

# TODO: unsupported feature
# statement ok
# ALTER TABLE t ALTER COLUMN j SET NOT NULL

# Failed to insert before drop constraint

# TODO: unsupported feature
# statement error
# INSERT INTO t VALUES (null)

# TODO: unsupported feature
# statement ok
# ALTER TABLE t ALTER COLUMN j DROP NOT NULL

# Insert after drop constraint

# TODO: unsupported feature
# statement ok
# INSERT INTO t VALUES (null)

# Only 1 null

query II
SELECT * FROM t WHERE j IS NULL
----
3 NULL

# test that SET NOT NULL preserves temporary flag

# TODO: unsupported feature
# statement ok
# CREATE TEMPORARY TABLE temp_not_null_test(x INTEGER)

# TODO: unsupported feature
# query TTI
# SELECT table_name, database_name, temporary FROM duckdb_tables() WHERE table_name='temp_not_null_test'
# ----
# temp_not_null_test	temp	1

# TODO: unsupported feature
# statement ok
# ALTER TABLE temp_not_null_test ALTER COLUMN x SET NOT NULL

# TODO: unsupported feature
# query TTI
# SELECT table_name, database_name, temporary FROM duckdb_tables() WHERE table_name='temp_not_null_test'
# ----
# temp_not_null_test	temp	1
