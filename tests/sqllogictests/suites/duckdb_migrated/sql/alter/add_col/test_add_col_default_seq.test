statement ok
DROP TABLE IF EXISTS test

statement ok
DROP SEQUENCE IF EXISTS seq

statement ok
CREATE SEQUENCE seq

statement ok
CREATE TABLE test(i INTEGER, j INTEGER)

statement ok
INSERT INTO test VALUES (1, 1), (2, 2)

# Databend supports sequence-backed defaults, but adding such a column to a non-empty
# table is rejected. Re-test with an empty table, then insert rows.
statement error
ALTER TABLE test ADD COLUMN m INTEGER DEFAULT nextval(seq)

query II
SELECT * FROM test
----
1 1
2 2

statement ok
DELETE FROM test

statement ok
ALTER TABLE test ADD COLUMN m INTEGER DEFAULT nextval(seq)

statement ok
INSERT INTO test(i, j) VALUES (1, 1), (2, 2)

query III
SELECT * FROM test ORDER BY i
----
1 1 1
2 2 2
