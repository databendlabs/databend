query T
select length(max_state(number).1), typeof(max_state(number)) from numbers(100);
----
9 TUPLE(BINARY, BOOLEAN)

query I
select length(sum_state(number).1), typeof(sum_state(number)) from numbers(10000);
----
8 TUPLE(BINARY, BOOLEAN)
