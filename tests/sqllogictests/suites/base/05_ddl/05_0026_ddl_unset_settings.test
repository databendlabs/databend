statement ok
UNSET sql_dialect

statement ok
UNSET timezone

query TTTT
SELECT name, value, default, level from system.settings where name in ('sql_dialect', 'timezone')
----
sql_dialect PostgreSQL PostgreSQL SESSION
timezone UTC UTC SESSION


statement ok
SET GLOBAL sql_dialect='MySQL'


statement ok
SET GLOBAL timezone='Asia/Shanghai'

onlyif mysql
query TTTT
SELECT name, value, default, level from system.settings where name in ('sql_dialect', 'timezone')
----
sql_dialect  MySQL  PostgreSQL  GLOBAL
timezone  Asia/Shanghai  UTC  GLOBAL

onlyif http
query TTTT
SELECT name, value, default, level from system.settings where name in ('sql_dialect', 'timezone')
----
sql_dialect  MySQL  PostgreSQL  SESSION
timezone  Asia/Shanghai  UTC  SESSION


statement ok
UNSET (timezone)


statement error 2801
UNSET stl_dialect


statement ok
UNSET sql_dialect


query TTTT
SELECT name, value, default, level from system.settings where name in ('sql_dialect', 'timezone')
----
sql_dialect PostgreSQL PostgreSQL SESSION
timezone                       UTC         UTC         SESSION
