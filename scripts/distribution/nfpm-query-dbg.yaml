name: "databend-query-dbg"
arch: "${deb_arch}"
platform: "linux"
version: "${deb_version}"
section: "database"
priority: "extra"
maintainer: "Databend Labs <opensource@databend.com>"
description: |
  Debug symbols for databend-query
vendor: "Databend Labs"
homepage: "https://databend.com"
license: "Apache-2.0"
depends:
  - databend-query
contents:
  # Binaries
  - src: ${path}/bin/databend-query.debug
    dst: /usr/bin/databend-query.debug
